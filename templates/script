sudo apt update && sudo apt upgrade -y
sudo apt install unzip nginx python3-pip -y

curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
unzip awscliv2.zip
sudo ./aws/install

TOKEN=$(curl -X PUT "http://169.254.169.254/latest/api/token" \
-H "X-aws-ec2-metadata-token-ttl-seconds: 21600")

curl -H "X-aws-ec2-metadata-token: $TOKEN" \
http://169.254.169.254/latest/meta-data/iam/security-credentials/

mkdir /flask-dashboard
cd /flask-dashboard

python3 -m venv venv
source venv/bin/activate

pip install flask pandas boto3 pyathena groq

/flask-dashboard

├── app.py
├── requirements.txt
├── Dockerfile
└── templates
    └── index.html

pip freeze > requirements.txt

nano test-athena.py

sudo apt install docker.io -y
sudo systemctl enable docker
sudo systemctl start docker
docker --version

nano Dockerfile
#build docker
docker build -t flask-dashboard .
docker images
#run
docker run -d \
-p 5000:5000 \
--name flask-dashboard \
flask-dashboard

docker ps

#github push
git init
git config --global user.name "Muhammad Revaldy"
git config --global user.email "muhammadrevaldy1688@gmail.com"
git add .
git commit -m "Initial Commit"
git branch -M main
git remote add origin https://github.com/muhammadrevaldy/ecommerce-realtime-dashboard.git
git push -u origin main

docker login ghcr.io -u muhammadrevaldy
docker tag flask-dashboard:latest \
ghcr.io/muhammadrevaldy/flask-dashboard:latest
docker push ghcr.io/muhammadrevaldy/flask-dashboard:latest
